import { LoginForm } from '@/components/LoginForm';

const Page = async () => (
    <main className="flex flex-col m-auto max-w-xs w-full h-full justify-center">
        <LoginForm />
    </main>
);

export default Page;
