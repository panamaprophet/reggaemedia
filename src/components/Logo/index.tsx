import { Logo as Icon } from '../Icons/Logo';

export const Logo = () => {
    return (
        <div className="flex items-center justify-center my-8">
            <Icon size={130} />
        </div>
    );
};
